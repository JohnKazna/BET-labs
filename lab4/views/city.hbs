<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Погода в {{weather.name}}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
<main>
    <div class="weather-card">
        <div class="weather-header">
            <img class="weather-icon" src="http://openweathermap.org/img/wn/{{weather.weather.[0].icon}}@2x.png" alt="{{weather.weather.[0].description}}"/>
            <div>
                <h2>{{weather.name}}</h2>
                <div class="weather-main">{{weather.main.temp}}°C</div>
                <div>{{weather.weather.[0].description}}</div>
            </div>
        </div>
        <div class="weather-detail">
            <div class="detail-item">
                <strong>Відчувається як:</strong> {{weather.main.feels_like}}°C
            </div>
            <div class="detail-item">
                <strong>Вітер:</strong> {{weather.wind.speed}} м/с
            </div>
            <div class="detail-item">
                <strong>Вологість:</strong> {{weather.main.humidity}}%
            </div>
            <div class="detail-item">
                <strong>Тиск:</strong> {{weather.main.pressure}} мм рт.ст.
            </div>
            <div class="detail-item">
                <strong>Видимість:</strong> {{weather.visibility}} км
            </div>
        </div>
    </div>
</main>
</body>
</html>